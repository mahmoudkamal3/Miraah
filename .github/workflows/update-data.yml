name: Update country data

on:
  schedule:
    - cron: "17 5 1 * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: country-data-update
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Run updater unit tests
        run: python -m unittest tests.test_update_world_bank -v

      - name: Dry-run validation
        run: python scripts/update_world_bank.py --dry-run

      - name: Snapshot homepage before write
        run: |
          cp public/index.html /tmp/miraah-homepage-before.html
          python - <<'PY'
          from pathlib import Path
          home = Path("public/index.html").read_text(encoding="utf-8")
          assert "home-hero" in home or "HOME_BOOT" in home
          assert "countrySearchA" not in home
          print("homepage marker ok")
          PY

      - name: Refresh World Bank data
        run: python scripts/update_world_bank.py --write

      - name: Verify comparison outputs and homepage safety
        run: |
          test -s public/compare/index.html
          test -s public/dashboard.html
          test -s public/index.html
          cmp -s public/compare/index.html public/dashboard.html
          cmp -s public/index.html /tmp/miraah-homepage-before.html
          python - <<'PY'
          from pathlib import Path
          import sys
          sys.path.insert(0, "scripts")
          import update_world_bank as wb
          home = Path("public/index.html").read_text(encoding="utf-8")
          assert "HOME_BOOT" in home or "home-hero" in home
          assert "countrySearchA" not in home
          html = Path("public/compare/index.html").read_text(encoding="utf-8")
          payload = wb.read_payload(html)
          assert "updatedAt" in payload
          assert len(payload["indicatorMeta"]) == 12
          assert len(payload["countries"]) >= 150
          print("embedded DATA ok; homepage untouched")
          PY

      - name: Commit only when comparison data changed
        run: |
          if git diff --quiet -- public/compare/index.html public/dashboard.html; then
            echo "No data changes"
            exit 0
          fi
          git config user.name "miraah-bot"
          git config user.email "actions@users.noreply.github.com"
          git add public/compare/index.html public/dashboard.html
          git commit -m "chore: refresh World Bank data"
          git push
